-- OLD nvim-cmp config - DISABLED in favor of blink.cmp
-- LazyVim now uses blink.cmp by default, so this is not needed
return {
  "hrsh7th/nvim-cmp",
  enabled = false, -- Disable this plugin completely
}
